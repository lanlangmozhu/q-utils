# Q-Utils 项目架构文档

## 📋 目录

1. [项目结构层级](#项目结构层级)
2. [文件依赖关系图](#文件依赖关系图)
3. [构建流程](#构建流程)
4. [测试流程](#测试流程)
5. [文档生成流程](#文档生成流程)
6. [文件更新影响链条](#文件更新影响链条)
7. [状态转换图](#状态转换图)

---

## 项目结构层级

```
q-utils/
├── 📁 配置层 (Configuration Layer)
│   ├── package.json          # 项目配置、依赖、脚本
│   ├── tsconfig.json         # TypeScript 编译配置
│   ├── rollup.config.mjs     # Rollup 构建配置
│   ├── jest.config.js        # Jest 测试配置
│   ├── typedoc.json          # TypeDoc 文档生成配置
│   ├── .eslintrc             # ESLint 代码检查配置
│   ├── .prettierrc           # Prettier 代码格式化配置
│   ├── .gitignore            # Git 忽略文件配置
│   └── .npmignore            # npm 发布忽略文件配置
│
├── 📁 源码层 (Source Layer)
│   └── src/
│       ├── index.ts          # 主入口文件（聚合所有模块）
│       └── [module]/         # 功能模块（20个模块）
│           ├── index.ts      # 模块入口（聚合子模块）
│           ├── *.ts          # 功能实现文件
│           ├── README.md     # 模块文档
│           └── __tests__/    # 测试文件
│               └── *.test.ts
│
├── 📁 构建层 (Build Layer)
│   └── dist/                 # 构建产物（由 rollup 生成）
│       ├── index.js          # CommonJS 格式
│       ├── index.esm.js      # ES Module 格式
│       ├── index.umd.js      # UMD 格式
│       ├── index.d.ts        # TypeScript 类型定义
│       └── *.map             # Source Map 文件
│
├── 📁 测试层 (Test Layer)
│   └── src/**/__tests__/     # 测试文件（与源码同目录）
│       └── *.test.ts         # Jest 测试用例
│
├── 📁 文档层 (Documentation Layer)
│   ├── docs/
│   │   ├── api/              # TypeDoc 生成的 API 文档
│   │   │   └── q-utils/      # 自动生成的文档结构
│   │   └── guide/            # VitePress 使用指南
│   └── README.md             # 项目主文档
│
├── 📁 示例层 (Example Layer)
│   └── examples/             # 使用示例
│       ├── index.html
│       └── server.js
│
└── 📁 工具层 (Tool Layer)
    ├── .github/              # GitHub Actions 配置
    └── .cursor/              # Cursor IDE 配置
```

---

## 文件依赖关系图

### 1. 源码依赖关系（自下而上）

```
┌─────────────────────────────────────────────────────────┐
│                    src/index.ts                         │
│              (主入口，聚合所有模块)                      │
└─────────────────────────────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ array/index  │ │ string/index │ │ object/index│ ... (20个模块)
└──────────────┘ └──────────────┘ └──────────────┘
         │               │               │
         ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ basic.ts     │ │ basic.ts     │ │ deepClone.ts │
│ compare.ts   │ │ format.ts    │ │ pick.ts      │
│ group.ts     │ │ encode.ts    │ │ ...          │
│ ...          │ │ ...          │ │              │
└──────────────┘ └──────────────┘ └──────────────┘
```

### 2. 配置依赖关系

```
package.json (根配置)
    │
    ├── rollup.config.mjs ──→ tsconfig.json ──→ src/**/*.ts
    │                           │
    │                           └──→ dist/**/*.js
    │
    ├── jest.config.js ──→ tsconfig.json ──→ src/**/__tests__/*.test.ts
    │
    └── typedoc.json ──→ src/index.ts ──→ docs/api/**/*.md
```

### 3. 构建依赖关系

```
rollup.config.mjs
    │
    ├── 输入: src/index.ts
    │
    ├── 配置: tsconfig.json
    │
    └── 输出: dist/
        ├── index.js (CJS)
        ├── index.esm.js (ESM)
        ├── index.umd.js (UMD)
        └── index.d.ts (类型定义)
```

---

## 构建流程

### 构建流程图

```
┌─────────────────┐
│  package.json   │
│  (scripts)      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  npm run build  │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────┐
│      rollup.config.mjs              │
│  ┌───────────────────────────────┐  │
│  │ 1. 读取 package.json           │  │
│  │ 2. 配置 TypeScript 编译        │  │
│  │ 3. 配置 Rollup 插件            │  │
│  └───────────────────────────────┘  │
└────────┬────────────────────────────┘
         │
         ├─────────────────┬─────────────────┐
         ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  TypeScript  │  │  CommonJS    │  │  UMD Bundle  │
│  Compiler    │  │  Plugin      │  │  Plugin      │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                  │                 │
       ▼                  ▼                 ▼
┌─────────────────────────────────────────────────┐
│              dist/ 目录                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │index.js  │  │index.esm │  │index.umd │      │
│  │(CJS)     │  │.js (ESM) │  │.js (UMD) │      │
│  └──────────┘  └──────────┘  └──────────┘      │
│  ┌──────────┐  ┌──────────┐                    │
│  │index.d.ts│  │*.map     │                    │
│  │(Types)   │  │(SourceMap)│                   │
│  └──────────┘  └──────────┘                    │
└─────────────────────────────────────────────────┘
```

### 构建步骤详解

1. **输入阶段**
   - `rollup.config.mjs` 读取 `package.json` 获取输出路径
   - 读取 `tsconfig.json` 获取 TypeScript 配置
   - 入口文件：`src/index.ts`

2. **编译阶段**
   - `rollup-plugin-typescript2` 编译 TypeScript
   - 生成 JavaScript 代码（移除 console、debugger）
   - 生成 Source Map

3. **打包阶段**
   - CommonJS 格式：`dist/index.js`
   - ES Module 格式：`dist/index.esm.js`
   - UMD 格式：`dist/index.umd.js`

4. **类型定义阶段**
   - `rollup-plugin-dts` 生成类型定义
   - 输出：`dist/index.d.ts`

---

## 测试流程

### 测试流程图

```
┌─────────────────┐
│  package.json   │
│  (test script)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  npm test       │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────┐
│      jest.config.js                  │
│  ┌───────────────────────────────┐  │
│  │ 1. 配置 ts-jest               │  │
│  │ 2. 配置 jsdom 环境             │  │
│  │ 3. 配置路径别名 @/             │  │
│  │ 4. 匹配测试文件                │  │
│  └───────────────────────────────┘  │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  src/**/__tests__/*.test.ts          │
│  ┌──────────┐  ┌──────────┐        │
│  │array/    │  │string/   │  ...   │
│  │__tests__ │  │__tests__ │        │
│  └──────────┘  └──────────┘        │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│          Jest Runner                 │
│  ┌───────────────────────────────┐  │
│  │ 1. 编译 TypeScript            │  │
│  │ 2. 执行测试用例                │  │
│  │ 3. 生成覆盖率报告              │  │
│  └───────────────────────────────┘  │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│      测试结果                        │
│  ┌──────────┐  ┌──────────┐        │
│  │通过/失败  │  │覆盖率报告 │        │
│  └──────────┘  └──────────┘        │
└─────────────────────────────────────┘
```

### 测试文件结构

```
src/
├── array/
│   ├── __tests__/
│   │   ├── index.test.ts      # 测试模块导出
│   │   ├── compare.test.ts    # 测试 compare.ts
│   │   ├── operation.test.ts  # 测试 operation.ts
│   │   └── random.test.ts     # 测试 random.ts
│   ├── index.ts
│   ├── compare.ts
│   └── operation.ts
```

---

## 文档生成流程

### 文档生成流程图

```
┌─────────────────┐
│  package.json   │
│  (docs script)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  npm run docs   │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────┐
│      typedoc.json                    │
│  ┌───────────────────────────────┐  │
│  │ 1. 入口: src/index.ts         │  │
│  │ 2. 输出: docs/api/             │  │
│  │ 3. 插件: markdown              │  │
│  │ 4. 排除: private/protected    │  │
│  └───────────────────────────────┘  │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│      TypeDoc 解析                    │
│  ┌───────────────────────────────┐  │
│  │ 1. 解析 TypeScript 源码        │  │
│  │ 2. 提取 JSDoc 注释             │  │
│  │ 3. 生成文档结构                │  │
│  └───────────────────────────────┘  │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│      docs/api/                      │
│  ┌───────────────────────────────┐  │
│  │ q-utils/                      │  │
│  │   └── namespaces/             │  │
│  │       ├── array/              │  │
│  │       │   └── functions/     │  │
│  │       │       ├── chunk.md   │  │
│  │       │       ├── unique.md  │  │
│  │       │       └── ...         │  │
│  │       └── string/             │  │
│  │           └── ...             │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

---

## 文件更新影响链条

### 影响链条图

```
┌─────────────────────────────────────────────────────────────┐
│                    文件更新影响链条                           │
└─────────────────────────────────────────────────────────────┘

1. 源码文件更新 (src/**/*.ts)
   │
   ├─→ 直接影响
   │   ├─→ 对应测试文件 (src/**/__tests__/*.test.ts)
   │   ├─→ 模块 index.ts (src/[module]/index.ts)
   │   └─→ 主入口 index.ts (src/index.ts)
   │
   ├─→ 间接影响（需要重新构建）
   │   ├─→ dist/**/*.js (构建产物)
   │   ├─→ dist/**/*.d.ts (类型定义)
   │   └─→ dist/**/*.map (Source Map)
   │
   └─→ 文档影响（需要重新生成）
       └─→ docs/api/**/*.md (API 文档)

2. 模块 index.ts 更新 (src/[module]/index.ts)
   │
   ├─→ 直接影响
   │   ├─→ 主入口 index.ts (src/index.ts)
   │   └─→ 对应测试文件 (src/[module]/__tests__/index.test.ts)
   │
   └─→ 间接影响
       ├─→ dist/**/*.js
       ├─→ dist/**/*.d.ts
       └─→ docs/api/**/*.md

3. 主入口 index.ts 更新 (src/index.ts)
   │
   ├─→ 直接影响
   │   └─→ 所有导出模块
   │
   └─→ 间接影响
       ├─→ dist/index.* (所有格式)
       └─→ docs/api/README.md

4. 配置文件更新
   │
   ├─→ tsconfig.json
   │   ├─→ 影响所有 TypeScript 编译
   │   └─→ 影响 dist/**/*.js 和 dist/**/*.d.ts
   │
   ├─→ rollup.config.mjs
   │   └─→ 影响 dist/**/*.js (构建产物)
   │
   ├─→ jest.config.js
   │   └─→ 影响测试执行
   │
   └─→ typedoc.json
       └─→ 影响 docs/api/**/*.md

5. package.json 更新
   │
   ├─→ 依赖更新
   │   ├─→ 需要重新安装 node_modules
   │   └─→ 可能影响构建和测试
   │
   ├─→ scripts 更新
   │   └─→ 影响构建/测试/文档生成流程
   │
   └─→ exports 更新
       └─→ 影响 npm 包的导出结构
```

### 具体影响场景

#### 场景 1: 更新功能文件 (src/array/basic.ts)

```
更新: src/array/basic.ts
  │
  ├─→ 必须更新
  │   └─→ src/array/__tests__/basic.test.ts (如果存在)
  │
  ├─→ 可能需要更新
  │   ├─→ src/array/index.ts (如果导出有变化)
  │   └─→ src/array/README.md (如果功能说明有变化)
  │
  └─→ 自动更新（运行命令后）
      ├─→ dist/array/basic.js (npm run build)
      ├─→ dist/array/basic.d.ts (npm run build)
      └─→ docs/api/q-utils/namespaces/array/functions/*.md (npm run docs)
```

#### 场景 2: 更新模块入口 (src/array/index.ts)

```
更新: src/array/index.ts
  │
  ├─→ 必须更新
  │   ├─→ src/index.ts (如果导出有变化)
  │   └─→ src/array/__tests__/index.test.ts
  │
  └─→ 自动更新
      ├─→ dist/array/index.* (所有格式)
      └─→ docs/api/q-utils/namespaces/array/README.md
```

#### 场景 3: 更新主入口 (src/index.ts)

```
更新: src/index.ts
  │
  ├─→ 必须检查
  │   └─→ 所有模块的导出是否正确
  │
  └─→ 自动更新
      ├─→ dist/index.* (所有格式)
      └─→ docs/api/q-utils/README.md
```

#### 场景 4: 更新配置文件

```
更新: tsconfig.json
  │
  └─→ 影响所有 TypeScript 文件
      ├─→ 需要重新构建 (npm run build)
      └─→ 可能影响测试 (npm test)

更新: rollup.config.mjs
  │
  └─→ 只影响构建
      └─→ 需要重新构建 (npm run build)

更新: jest.config.js
  │
  └─→ 只影响测试
      └─→ 需要重新运行测试 (npm test)
```

---

## 状态转换图

### 开发状态转换

```
┌─────────────┐
│  开发状态    │
│ (Development)│
└──────┬──────┘
       │
       ├─→ 编写代码 (src/**/*.ts)
       │   │
       │   ├─→ 运行测试 (npm test)
       │   │   │
       │   │   └─→ 测试失败 ──→ 修复代码 ──→ 重新测试
       │   │   │
       │   │   └─→ 测试通过 ──→ 继续开发
       │   │
       │   └─→ 代码检查 (npm run lint)
       │       │
       │       └─→ 格式化 (npm run format)
       │
       ├─→ 构建 (npm run build)
       │   │
       │   └─→ 生成 dist/**/*
       │
       └─→ 生成文档 (npm run docs)
           │
           └─→ 生成 docs/api/**/*
```

### 发布状态转换

```
┌─────────────┐
│  准备发布    │
│ (Pre-Release)│
└──────┬──────┘
       │
       ├─→ 1. 运行测试 (npm test)
       │   │
       │   └─→ 测试通过 ──→ 继续
       │   └─→ 测试失败 ──→ 修复 ──→ 重新测试
       │
       ├─→ 2. 代码检查 (npm run lint)
       │   │
       │   └─→ 检查通过 ──→ 继续
       │   └─→ 检查失败 ──→ 修复 ──→ 重新检查
       │
       ├─→ 3. 构建 (npm run build)
       │   │
       │   └─→ 构建成功 ──→ 继续
       │   └─→ 构建失败 ──→ 修复 ──→ 重新构建
       │
       ├─→ 4. 更新版本号 (npm version)
       │   │
       │   └─→ 自动提交和推送
       │
       └─→ 5. 发布到 npm (npm publish)
           │
           └─→ 发布成功 ──→ 完成
```

### 文件状态转换

```
源码文件状态:
┌──────────┐    修改      ┌──────────┐    构建      ┌──────────┐
│  未修改   │ ────────→ │  已修改   │ ────────→ │  已构建   │
└──────────┘            └──────────┘            └──────────┘
                              │                       │
                              │                       │
                              ▼                       ▼
                        ┌──────────┐            ┌──────────┐
                        │  测试中   │            │  已发布   │
                        └──────────┘            └──────────┘
```

---

## 快速参考：文件更新检查清单

### 更新功能文件时

- [ ] 更新对应的测试文件
- [ ] 检查模块 index.ts 是否需要更新导出
- [ ] 更新模块 README.md（如果有功能变化）
- [ ] 运行 `npm test` 确保测试通过
- [ ] 运行 `npm run build` 重新构建
- [ ] 运行 `npm run docs` 更新文档（可选）

### 更新模块入口时

- [ ] 检查主入口 src/index.ts 是否需要更新
- [ ] 更新模块测试文件
- [ ] 运行 `npm test` 确保测试通过
- [ ] 运行 `npm run build` 重新构建

### 更新主入口时

- [ ] 检查所有模块导出是否正确
- [ ] 运行 `npm test` 确保测试通过
- [ ] 运行 `npm run build` 重新构建

### 更新配置文件时

- [ ] 根据配置类型运行相应命令
  - `tsconfig.json` → `npm run build` + `npm test`
  - `rollup.config.mjs` → `npm run build`
  - `jest.config.js` → `npm test`
  - `typedoc.json` → `npm run docs`

---

## 总结

### 核心原则

1. **自下而上依赖**：功能文件 → 模块入口 → 主入口
2. **测试驱动**：每次更新功能文件都要更新测试
3. **构建验证**：更新后必须重新构建验证
4. **文档同步**：重要功能更新后同步文档

### 关键路径

```
开发 → 测试 → 构建 → 文档 → 发布
  ↑      ↑      ↑      ↑      ↑
  └──────┴──────┴──────┴──────┘
        反馈循环
```

---

*最后更新: 2024年*

